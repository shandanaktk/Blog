<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-screen bg-zinc-800 text-white p-10">
      <div class="w-full flex justify-between items-center">
        <h1 class="text-3xl tracking-tighter">Edit Bar</h1>
        <a
          class="bg-red-600 w-40 text-center rounded-lg px-5 py-3 text-sm"
          href="/logout"
          >Logout</a
        >
      </div>

      <hr class="my-5" />
      <h5>Edit Your Post</h5>

      <form method="post" action="/update/<%= post._id %>">
        <textarea
          class="text-white p-3 resize-none w-1/3 mt-5 mb-2 bg-transparent border-2 border-zinc-600 rounded-lg"
          placeholder="What's on your mind?"
          name="content"
        >
<%= post.content %></textarea
        >
        <input
          class="block w-40 font-bold text-black px-5 py-3 bg-yellow-600 rounded-lg"
          type="submit"
          value="Save Changes"
        />
      </form>
    </div>
  </body>
</html>
